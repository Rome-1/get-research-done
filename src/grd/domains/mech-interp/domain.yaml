name: mech-interp
display_name: Mechanistic Interpretability
description: >
  Mechanistic interpretability research: circuit discovery, feature extraction,
  activation patching, superposition analysis, SAE training, and causal tracing
  in neural networks.
version: 1

conventions_file: conventions/convention-fields.yaml
subfields_dir: subfields
protocols_dir: protocols
errors_dir: errors

branding:
  tagline: "Reverse-engineering neural computation"
  color: purple
