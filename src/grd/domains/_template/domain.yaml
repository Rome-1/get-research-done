name: my-domain
display_name: My Research Domain
description: >
  Description of this research domain and what conventions/protocols it provides.
version: 1

conventions_file: conventions/convention-fields.yaml

# Declare active content types and their directory paths (relative to pack root).
# Only list content types that this domain actually provides.
content:
  subfields: subfields
  protocols: protocols
  errors: errors

# Result metadata fields — domain-specific fields for intermediate results.
# Each entry defines a --meta key that users can pass to `grd result add/update`.
# result_metadata_fields:
#   - key: equation
#     label: "LaTeX equation"
#     help: "LaTeX equation for this result"

branding:
  tagline: "Your tagline here"
