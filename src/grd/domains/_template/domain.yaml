name: my-domain
display_name: My Research Domain
description: >
  Description of this research domain and what conventions/protocols it provides.
version: 1

conventions_file: conventions/convention-fields.yaml

# Declare active content types and their directory paths (relative to pack root).
# Only list content types that this domain actually provides.
content:
  subfields: subfields
  protocols: protocols
  errors: errors

branding:
  tagline: "Your tagline here"
